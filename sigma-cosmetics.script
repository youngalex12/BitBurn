while (true) {
if (getServerSecurityLevel('sigma-cosmetics') > getServerMinSecurityLevel('sigma-cosmetics')) {
    weaken('sigma-cosmetics');
} else {
    if (getServerMoneyAvailable('sigma-cosmetics') < getServerMaxMoney('sigma-cosmetics')) {
        grow('sigma-cosmetics');
    } else {
        hack('sigma-cosmetics');
    }

}
}
