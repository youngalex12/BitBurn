while (true) {
if (getServerSecurityLevel('neo-net') > getServerMinSecurityLevel('neo-net')) {
    weaken('neo-net');
} else {
    if (getServerMoneyAvailable('neo-net') < getServerMaxMoney('neo-net')) {
        grow('neo-net');
    } else {
        hack('neo-net');
    }

}
}
